/** 
 * Declaration for app info object injected by webpack
 */
declare var appInfo: {
    gitVersion: string,
    buildVersion: string,
    name: string,
    description: string,
    version: string
};